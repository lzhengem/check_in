<p id="notice"><%= notice %></p>

<h1>Listing Check Ins</h1>

<table>
  <thead>
    <tr>
      <th>Location</th>
      <th>Longitude</th>
      <th>Latitude</th>
      <th>Destination</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @check_ins.each do |check_in| %>
      <tr>
        <td><%= check_in.location %></td>
        <td><%= check_in.longitude %></td>
        <td><%= check_in.latitude %></td>
        <td><%= check_in.destination %></td>
        <td><%= link_to 'Show', check_in %></td>
        <td><%= link_to 'Edit', edit_check_in_path(check_in) %></td>
        <td><%= link_to 'Destroy', check_in, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Check in', new_check_in_path %>
